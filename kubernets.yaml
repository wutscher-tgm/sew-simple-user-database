apiVersion: apps/v1
kind: Deployment
metadata:
  name: sew-sud-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sew-sud-server
  template:
    metadata:
      labels:
        app: sew-sud-server
    spec:
      containers:
        - name: sew-sud-server
          image: rwutscher/sew-sud-server:0.3
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: sew-sud-server-service
  labels:
    app: sew-sud-server
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
  selector:
    app: sew-sud-server